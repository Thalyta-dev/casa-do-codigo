micronaut:
  application:
    name: casaDoCodigo
datasources:
  default:
    url: jdbc:mysql://localhost/casaDoCoDigo2?createDatabaseIfNotExist = true
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    schema-generate: CREATE_DROP
    dialect: mysql
jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true
